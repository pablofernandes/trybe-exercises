/**
 * This is a exercise program to compare three numbers named a and b and print the result
 * on the console using console.log() function.
 * 
 * By Pablo P Fernandes
 * 
 */

 let a = 111;
 let b = 112;
 let c = 111;

 if(a >= b && a >= c){
     console.log(a + ' is the bigger number.');
 }else if(b >= a && b >= c){
     console.log(b + ' is the bigger number.')
 }else{
     console.log(c + ' is the bigger number.')
 }
