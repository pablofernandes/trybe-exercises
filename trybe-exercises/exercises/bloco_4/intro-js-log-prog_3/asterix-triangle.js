/**
 * This is an exercise program to print a triangle of asterix from a given number.
 * 
 * By Pablo P Fernandes
 */

let asterix = '';
let number = 50;

for (let index = 0; index < number; index += 1) {
    asterix += '*';
    console.log(asterix); 
}
