let skills = ['Javascript', 'HTML', 'GitHub', 'English', 'Programing Logic'];
let phrase = 'Hi x, how are you?'
let functionOne = (name, phrase) => phrase.replace('x', name);

let student = 'Pablo';
let functionTwo = (skills,) => {
    let finalPhrase = ;
    for (let index = 0; index < skills.length; index++) {
         ; 
    }
}